<Page
    x:Class="_24102019_uwp.Views.MiscellaneousPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_24102019_uwp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Padding="24">

        <TextBlock FontWeight="Bold" Margin="0,8,0,0">
                Select Type
        </TextBlock>

        <ComboBox Name="cbTypes" PlaceholderText="Pick a title" Margin="0,12,0,0" Width="250" SelectionChanged="loadInfo">
        </ComboBox>

        <TextBlock FontWeight="Bold" Margin="0,48,0,0">
            Set the rental rate
        </TextBlock>
        <TextBox Name="RentalRate" Margin="0,12,0,0" Width="250" HorizontalAlignment="Left" PlaceholderText="Input new rental rate here">
        </TextBox>

        <TextBlock FontWeight="Bold" Margin="0,24,0,0">
            Set the rental period
        </TextBlock>
        <TextBox Name="RentalPeriod" Margin="0,12,0,0" Width="250" HorizontalAlignment="Left" PlaceholderText="Input new rental period here">
        </TextBox>

        <Button Margin="0,24,0,0" Click="Save">
            Save
        </Button>
    </StackPanel>
</Page>
